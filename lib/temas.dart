import 'package:flutter/material.dart';

// Tema claro
final lightTheme = ThemeData(
  colorScheme: ColorScheme.light(
    primary: Colors.deepOrangeAccent, // Color principal en modo claro
    background: Colors.white, // Color de fondo en modo claro
    // Agrega más configuraciones de colores según tus preferencias
  ),// Color de fondo en modo claro
  // Agrega más configuraciones según tus preferencias
);

// Tema oscuro
final Darktheme = ThemeData(
  colorScheme: ColorScheme.light(
    primary: Colors.orangeAccent, // Color principal en modo claro
    background: Colors.black, // Color de fondo en modo claro
    // Agrega más configuraciones de colores según tus preferencias
  ),// Color de fondo en modo claro
  // Agrega más configuraciones según tus preferencias
);